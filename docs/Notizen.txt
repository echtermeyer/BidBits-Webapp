# TODOs
- mehr Sachen in der db
- Ranking ist 1-5, nicht 1-10
- password hashes

## Backend
Eine Stored Procedure process_payment, die immer dann getriggered wird wenn eine Auktion ausläuft und daraufhin einen Eintrag in der Tabelle payment macht.


# Fragen an Dozent
- Watchlist ist jetzt eine weak entity die kein eigenes attribut hat. Müssen ihn mal fragen, ob das geht. Habe online keine vergleichbaren Fälle gefunden.


# Notiz von David an David
- Wenn's nach dem Pullen beim Ausführen des Scripts einen Error gibt, erstmal den Port in der config Datei auf 5432 ändern. Angleichung des Ports in pgAdmin hat nicht funktioniert.


----
DONE
won_auction -> jetzt auch eigene verkäufe
Fehler beim anlegen eines items (item_id duplicate, error meldung als comment über method) 
Mehr Beispiele dumpen
feedback methode
payment insert -> database manager --> TESTEN
Stored procedure -> wenn user angelegt wird , items auf die watchlist

-----------

Gemeinsame Doku

3.1. NFs mit ChatGPT

3.5. Document queries mit ChatGPT

3.6. Index (erstmal warten bis er es uns erklärt)

------------

NOchmal checken ob ERD-Diagram == DDL 

